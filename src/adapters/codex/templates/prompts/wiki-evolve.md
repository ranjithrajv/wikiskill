Run `npx wikiskill evolve-prompt` and follow the printed instructions exactly,
step by step, using your file tools. When every step is complete, run
`npx wikiskill evolve-complete` to close out the iteration.
